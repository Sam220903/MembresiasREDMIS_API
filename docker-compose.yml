services: 
  db:
    image: mysql
    volumes:
      - db_data:/var/lib/mysql
      - ./db/MR_DB_V4.sql:/docker-entrypoint-initdb.d/MR_DB_V4.sql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: REDMIS
      MYSQL_DATABASE: mr_db
      MYSQL_USER: mr_user
      MYSQL_PASSWORD: REDMIS
    ports:
      - "3306:3306"
volumes:
  db_data:
